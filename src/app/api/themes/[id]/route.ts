import { NextResponse } from "next/server";

// GET /api/themes/:id - Theme detail with linked insights
export async function GET() {
  return NextResponse.json({ error: "Not implemented" }, { status: 501 });
}
